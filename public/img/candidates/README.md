# Candidate photos

Drop portrait photos here, named to match `photo` in `data/candidates.tsx`:

```
01.jpg, 02.jpg, … 30.jpg
```

- Portrait orientation, roughly 3:4 aspect ratio (e.g. 900×1200).
- Any missing file falls back to the candidate's initials on a blue background.
